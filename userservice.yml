server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userservicedb
    username: root
    password: Durga@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
